# Workspace packages are linked, never fetched from a registry.
link-workspace-packages=true
# A lockfile mismatch must fail CI rather than silently resolving new versions.
strict-peer-dependencies=false
