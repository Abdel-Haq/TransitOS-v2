// Phase 0.2. DTOs, the French error catalog, the capability registry and the action
// registry. Everything else is generated from or validated against this package, so it
// is written before any table exists. See docs/00-PLAN.md §Phase 0.
export {};
